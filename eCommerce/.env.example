# Kopiraj ovaj fajl u ".env" u istom folderu (pored docker-compose.yml) i popuni stvarnom lozinkom.
# Mora se poklapati sa lozinkom u LuxSalon.WebAPI/.env (ConnectionStrings__DefaultConnection).
SA_PASSWORD=YOUR_SQL_PASSWORD
